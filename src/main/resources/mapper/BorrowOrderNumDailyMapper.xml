<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianli.borrow.dao.BorrowOrderNumDailyMapper">
    <select id="selectCountByDate" resultType="integer">
        select ifnull(sum(order_num),0)
        from borrow_order_num_daily
        <where>
            <if test="startDate != null">
                and statistical_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                and statistical_date &lt;= #{endDate}
            </if>
        </where>
    </select>
</mapper>
